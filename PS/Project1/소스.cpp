#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;
int N, K;
int dp[15][2 << 15];
int main()
{
	cin >> N;
	vector<string> v(N);
	for (int i = 0; i < N; i++) cin >> v[i];
	cin >> K;
	
}